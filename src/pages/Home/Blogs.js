import { Box } from '@mui/material';
import React from 'react';

const Blogs = () => {
    return (
        <Box sx={{width: '100%'}}>
            hello
        </Box>
    );
};

export default Blogs;